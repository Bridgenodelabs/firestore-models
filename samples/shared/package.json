{
  "name": "@firestore-type/sample-shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared sample Task model for firestore-type",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bridgenodelabs/firestore-models": "file:../.."
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
